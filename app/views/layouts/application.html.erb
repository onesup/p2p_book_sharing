<!DOCTYPE html>
<html>
<head>
  <title>Web</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div class="container master-container">
		<h1>
			<%= yield :village %>
			<%= link_to "아파트 도서관", root_path %>
		</h1>
		<ul class="membership">
			<% if logged_in? %>
				<li><%= link_to "로그아웃", logout_path %></li>
			<% else %>
				<% unless params[:action] == 'si_list' or params[:action] == 'gu_list' or params[:action] == 'dong_list' or params[:action] == 'village_list'%>
					<li><%= link_to "참여하기", signup_path %></li>
				<% end %>
				<li><%= link_to "로그인", login_path %></li>
			<% end %>
		</ul>
		<%= yield %>
		<footer>
		<p><%= link_to "제작자에게 기능 제안하기", "https://www.facebook.com/groups/518765154800623/" %></p>
		</footer>
	</div>
</body>
</html>
