<%= form_for(@book_url) do |f| %>
  <% if @book_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_url.errors.count, "error") %> prohibited this book_url from being saved:</h2>

      <ul>
      <% @book_url.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
